export enum FORECAST_TYPES {
    ONE_CALL = 'one-call',
    CURRENT_CITY_WEATHER = 'current-city-weather'
}

export enum TEMP_TYPE {
    CURRENT = 'current',
    HOURLY = 'hourly',
    DAILY = 'daily'
}